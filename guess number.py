#guess the right no. between 1 and 10

import random
num, num_guess=random(1, 11)
    if
       num = num_guess
       num_guess = int(input('guess a number between 1 and 10 :'))
#now printing what we require
       print('right guess') 
    else:
           print('wrong guess, keep guessing')